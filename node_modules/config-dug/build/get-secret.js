"use strict";
/* eslint-disable no-console */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const aws_param_store_1 = __importDefault(require("aws-param-store"));
const getSecret = (secretName, region, timeout) => {
    try {
        const secret = aws_param_store_1.default.getParameterSync(`/aws/reference/secretsmanager/${secretName}`, {
            region,
            httpOptions: {
                timeout,
            },
        });
        return JSON.parse(secret.Value);
    }
    catch (error) {
        console.error('ERROR: Unable to get secret from AWS Secrets Manager', {
            secretName,
            region,
            timeout,
        });
        console.error(error);
        return {};
    }
};
exports.default = getSecret;
//# sourceMappingURL=get-secret.js.map